#pragma once
#include <string>

class Projectile {
public:
    std::string name;
    double velocity;
    std::string imagePath;
    // Add more fields if needed for arch calculation
    // Future me: Not happening
};